{
   "$schema":       "http://json-schema.org/draft-03/schema#",
   "id":            "/rvbd/schema/tm/1.0/config/active/ssl/cas#read",
   "title":         "SSL Trusted Certificate",
   "description":   "SSL certificate authority certificates (CAs) and certificate revocation lists (CRLs) can be used when validating server and client certificates.",
    "javaType" :     "org.rackspace.stingray.client.ssl.cacrl.SslCacrlRead",
   "type":          "object",
   "required":      true,
   
   "properties": {
      "properties": {
          "javaType" :     "org.rackspace.stingray.client.ssl.cacrl.SslCacrlReadProperties",
         "type":                  "object",
         "required":              true,
         "additionalProperties":  true,
         
         "properties": {
            
         }
      }
   }
}
